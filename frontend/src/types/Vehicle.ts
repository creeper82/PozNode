export default interface Vehicle {
    id: string;

    airConditioned?: boolean;
    bike?: boolean;
    chargers?: boolean;
    lowFloor?: boolean;
    lowEntrance?: boolean;
    ramp?: boolean;
    ticketMachine?: boolean;
}